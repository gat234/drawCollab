<%- include('../partial/form-partial-t.ejs',{title:"Log In",lan:lan}) %>
                <h1 class="h3 mb-3 fw-normal text-center">
                    <% if (lan=="lv") { %>
                    Ielogoties
                    <% } else { %>
                    Log In
                    <% } %>
                </h1>
                <input type="hidden" value="<%=rand%>" name="randcheck" />
                <div class="form-floating">
                <input type="text" name="name" value="<%=name%>" class="form-control" id="floatingInput" placeholder="name@example.com">
                <label for="floatingInput">
                    <% if (lan=="lv") { %>
                    Epasts vai Vārds
                    <% } else { %>
                    Email or Username
                    <% } %>
                </label>
                <span class="error"><%=nameErr%></span>
                </div>
<%- include('../partial/form-partial-b.ejs',{lan:lan}) %>

